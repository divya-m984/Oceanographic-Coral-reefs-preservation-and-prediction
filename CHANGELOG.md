# CHANGELOG

> **Project:** Oceanographic: A Machine Learning-Driven Sonar Framework for
> Real-Time Coral Reef Habitat Prediction and Marine Ecosystem Monitoring

All notable changes are listed by milestone. Dates are not included because
this is a college project without fixed delivery dates for individual
milestones. Commit SHAs are provided for exact traceability.

---

## [Unreleased] — Dashboard frontend redesign

### Changed
- **Background is now licensed footage, not generated artwork.** The hand-authored
  animated SVG ocean (`src/dashboard/seascape.py`, ~4,300 animated paths per page)
  and the bordered hero card (`src/dashboard/hero.py`) are removed. The backdrop is
  one `<video>` on the landing and one pre-graded still elsewhere —
  `src/dashboard/cinema.py` + `src/dashboard/media.py`.
- **Navigation moved from the sidebar to a top bar.** `src/dashboard/app.py` is now
  an `st.navigation(position="top")` router declaring pages with `st.Page`.
- **`src/dashboard/pages/` renamed to `src/dashboard/views/`.** Required: a `pages/`
  folder beside the entrypoint is auto-discovered and would render a second,
  competing navigation.

  **URLs are unchanged.** Each `st.Page` pins the `url_path` that auto-discovery
  used to derive from the filename: `/Overview`, `/Reef_Map`, `/Habitat_Health`,
  `/Restoration_Planning`, `/Predict`, `/Model_Performance`, `/MLOps_Status`,
  `/Drift_Monitoring`, `/Governance`. The landing is the default page at `/`.

  Callers that referenced page *files* must update: `src/dashboard/pages/X.py` ->
  `src/dashboard/views/X.py`. This affected the release workflow's page-render
  sweep and the test suite.
- `components.set_page()` no longer calls `st.set_page_config` — under
  `st.navigation` the entrypoint owns it, and a second call raises. It now injects
  the theme and renders the background stage, and takes a `motion=` flag that only
  the landing sets.
- The sidebar is filters-only; brand and service status left the rail.

### Added
- `src/dashboard/static/media/` — CC BY underwater footage, poster and reef still,
  with `ATTRIBUTION.md` recording source, creator, licence, URL and changes made.
- `server.enableStaticServing = true` in `.streamlit/config.toml`.

## M14 — Release Readiness and Course Evidence

**Commit:** (see `git log --oneline -1`)

### Added
- `Makefile` — safe build/test/demo targets; narrowly scoped cleanup
- `scripts/preflight.py` — read-only preflight checker (18 checks, JSON output, exit code)
- `scripts/demo.py` — demo orchestrator (`start`, `status`, `verify`, `stop`)
- `scripts/collect_evidence.py` — generates `reports/project_manifest.json`
- `docs/architecture.md` — full system architecture with Mermaid-style diagrams
- `docs/course_evidence.md` — factual course evidence (activities 2–7, levels 0–3)
- `docs/demo_guide.md` — 8–12 minute classroom demonstration guide
- `CHANGELOG.md` — milestone history (this file)
- `tests/test_demo.py` — tests for preflight, demo orchestrator, manifest schema
- `src/dashboard/pages/9_Governance.py` — read-only governance Streamlit page
- `reports/project_manifest.json` — evidence manifest (generated by collect_evidence.py)
- Updated `README.md` with quick-start, Makefile, architecture links, disclaimer
- Updated `docs/progress.md` with M14 section

### Design rules enforced
- No target in the Makefile trains, registers, promotes, or rolls back a model
- `make clean-generated` never deletes data/raw/, models/, artifacts/, .dvc/
- `scripts/preflight.py` is read-only; never modifies any project artifact
- `scripts/demo.py` never calls fit, register, or promote; Docker data is preserved on stop
- `scripts/collect_evidence.py` is read-only; no MLflow mutation

---

## M13 — Controlled Retraining and Model Governance

**Commit:** 8268e13

### Added
- `src/models/retrain.py` — validates labelled input (8-check contract), trains challenger
- `src/models/compare.py` — champion-challenger comparison engine (4 gates)
- `src/models/promote.py` — explicit promotion requiring `--approve --approver --reason`
- `src/models/rollback.py` — champion rollback with `--dry-run` support
- `src/models/model_card.py` — Markdown model card generator
- `scripts/run_retraining.py` — CLI orchestrator (retrain + compare; never promotes)
- `tests/test_retraining.py` — 70 tests
- `params.yaml` `retraining:` section with comparison thresholds
- `src/config.py` — `cfg.retraining` property

### Design rules enforced
- Unlabelled production drift data rejected at validation stage
- Champion alias never moved automatically
- Every promotion writes a timestamped receipt
- Every rollback writes a timestamped receipt
- Canonical DB never touched by any M13 test (all use temp mlflow_uri)

---

## M12 — Docker Orchestration and Portable Model Bundles

**Commit:** 142dff2

### Added
- `Dockerfile.api` — multi-stage, non-root `coralsense` user, health check
- `Dockerfile.dashboard` — multi-stage, non-root user, health check
- `Dockerfile.mlflow` — multi-stage, non-root user, health check
- `docker-compose.yml` — 4 services (mlflow:5000, api:8000, dashboard:8501, drift profile)
- `docker/init_mlflow.sh` — copies canonical DB to named volume, rewrites paths, starts server
- `docker/init_drift.sh` — one-shot drift container entrypoint
- `scripts/export_champions.py` — exports champion joblibs to `deploy/bundles/` with SHA-256
- `scripts/verify_deployment_bundle.py` — 11-check bundle verification CLI
- `src/api/bundle_loader.py` — `BundleInferencePipeline` (same interface, checksum-verified)
- `src/api/model_loader.py` — dual-mode via `CORALSENSE_MODEL_MODE` env var
- `tests/test_docker.py` — 82 tests
- `tests/test_bundle.py` — 52 tests
- `.env.example` updated with Docker-specific variables

### Design rules enforced
- `artifacts/mlruns.db` mounted `:ro` — never modified at runtime
- API does not depend_on mlflow service (bundle mode)
- Named volume `mlflow-runtime` holds runtime DB copy; canonical DB is never written

---

## M11 — Evidently Drift Monitoring

**Commit:** 8b10355

### Added
- `src/monitoring/generate_production.py` — reference and shifted production window generator
- `src/monitoring/drift.py` — `DriftDetector` (feature drift, prediction drift, confidence drift)
- `src/monitoring/run_drift.py` — CLI (`--shift-scale`, `--no-html`)
- `src/dashboard/pages/8_Drift_Monitoring.py` — dashboard drift page
- `dvc.yaml` — `run_drift` stage added (7th stage)
- `tests/test_monitoring.py` — 68 tests
- `params.yaml` `monitoring:` section (reference_n, production_n, shift_scale, drift_threshold)

### Drift design
- Standard shift (scale=1.0): +3°C temperature, +20pp bleaching, −15pp coral cover, +5 NTU turbidity
- Zero shift: 0 features drift (baseline)
- Standard shift: 4 features drift, RETRAIN recommended
- Prediction drift: chi2_contingency (not chisquare — avoids floating-point tolerance issues)
- Confidence drift: KS two-sample test

---

## M10 — Streamlit Monitoring Dashboard

**Commit:** ca9e5bb

### Added
- `src/dashboard/app.py` — home page, project overview
- `src/dashboard/api_client.py` — `APIClient` with `APIError`; reads `CORALSENSE_API_URL`
- `src/dashboard/data_loader.py` — `@st.cache_data`, read-only data access
- `src/dashboard/components.py` — colour palettes, `render_sidebar`, `build_predict_payload`
- `src/dashboard/pages/1_Overview.py` through `8_Drift_Monitoring.py` — 8 pages
- Geographic map using `px.scatter_map` + `map_style="open-street-map"` (Plotly 6, no API key)
- `.streamlit/config.toml` — dark ocean navy theme
- `tests/test_dashboard.py` — 133 tests

---

## M9 — FastAPI Inference Service

**Commit:** ecfdf5c

### Added
- `src/api/schemas.py` — Pydantic v2 `ObservationInput` (extra="forbid", finite-float guard)
- `src/api/model_loader.py` — `ModelLoader` singleton (graceful load, no fit/register)
- `src/api/main.py` — 7-endpoint FastAPI app, lifespan startup, `Annotated` dependency
- `tests/test_api.py` — 83 tests

### Endpoints
- `GET /` — project index
- `GET /health` — liveness and model readiness
- `GET /model-info` — champion metadata (safe fields only)
- `POST /predict/reef-health` — single reef-health prediction
- `POST /predict/restoration` — single restoration prediction
- `POST /predict/both` — dual prediction
- `POST /predict/batch` — batch predictions (max 50)

---

## M8 — CI/CD Pipeline

### Added
- `.github/workflows/ci.yml` — 5-job GitHub Actions workflow
- `scripts/ci_check.sh` — local CI equivalent
- `scripts/ci_smoke_test.py` — 500-row ML smoke test
- `scripts/ci_validate_pipeline.py` — DVC structure validation
- `tests/test_ci_workflow.py` — 26 tests

### CI jobs
1. `code-quality` — ruff check + ruff format --check
2. `tests` — full test suite, disposable MLflow DB
3. `pipeline-validation` — DVC YAML + isolated data round-trip
4. `ml-smoke-test` — 500-row training verification
5. `build` — wheel + sdist build

---

## M7 — DVC Pipeline Automation

### Added
- `dvc.yaml` — 6-stage DAG (generate → validate → preprocess → train → evaluate → register_candidate)
- `src/models/run_evaluate.py` — reads evaluation JSONs → `reports/metrics.json`
- `src/models/run_register_candidate.py` — registers candidate (no champion alias)
- `tests/test_dvc_pipeline.py` — 26 tests
- DVC initialised; `data/processed/` moved from git to DVC tracking
- `.dvcignore` for `artifacts/mlruns.db` and `mlruns/`

---

## M6 — MLflow Model Registry

### Added
- `src/models/registry.py` — `ModelRegistry` class
- `src/models/predict.py` — champion-loading inference helpers
- Quality gates in `params.yaml` (`quality_gates:` section)
- `tests/test_registry.py` — 39 tests
- `tests/test_predict.py` — 49 tests
- Champion alias v1 set for both models

---

## M5 — Model Training and MLflow Tracking

### Added
- `src/models/train.py` — training loop (LR, RF, XGBoost) with MLflow auto-logging
- `src/models/evaluate.py` — classification report, confusion matrix, feature importance
- `tests/test_models.py`

### Champion models (SYNTHETIC DATA)
- Reef health: Logistic Regression, CV macro-F1 = 0.7612, test macro-F1 = 0.7871
- Restoration: XGBoost, CV macro-F1 = 0.7913, test macro-F1 = 0.8029

---

## M4 — Preprocessing and Feature Engineering

### Added
- `src/data/preprocess.py` — stratified 80/20 split, ColumnTransformer (StandardScaler + OHE)
- `src/features/build_features.py` — 6 derived features
- `tests/test_preprocess.py`

---

## M3 — Dataset Validation

### Added
- `src/data/validate.py` — Pandera schema with range checks, regex, allowed values
- `tests/test_validate.py`

---

## M2 — Synthetic Dataset

### Added
- `src/data/generate_data.py` — reproducible synthetic generator (seed=42)
- 15,000 observations, 21 columns, 4 Indian reef regions
- `tests/test_generate_data.py`

---

## M1 — Project Foundation

### Added
- Python 3.14 virtual environment (`.venv/`)
- `pyproject.toml` — build config, pytest settings, ruff configuration
- `src/config.py` — typed `Config` dataclass, absolute path resolution
- `params.yaml` — single source of truth
- `requirements.txt`, `requirements-dev.txt`
- `.env.example`, `.gitignore`, `.dvcignore`
- `tests/test_config.py`
- `.github/workflows/ci.yml` scaffold
